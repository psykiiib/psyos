import original from 'react95/dist/themes/original';

const psyTheme = {
  ...original,
  // We don't need to override anything for the authentic look
  // but we ensure the desktop background is accessible here if needed.
  desktopBackground: '#008080', // Classic Windows 95 Teal
};

export default psyTheme;